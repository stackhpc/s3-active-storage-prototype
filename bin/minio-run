#!/usr/bin/env bash

REPO_ROOT="$(dirname $(dirname $(realpath ${BASH_SOURCE[0]:-${(%):-%x}})))"

exec docker run \
  --rm \
  -p 9000:9000 \
  -p 9001:9001 \
  -v "$REPO_ROOT/testdata:/data" \
  minio/minio \
    server \
    /data \
    --console-address ":9001"
